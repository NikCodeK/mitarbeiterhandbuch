{
  "name": "mitarbeiterhandbuch",
  "private": true,
  "scripts": {
    "vercel-build": "cd handbuch-app && npm install && npm run build",
    "dev": "cd handbuch-app && npm run dev",
    "build": "cd handbuch-app && npm run build",
    "start": "cd handbuch-app && npm run start"
  }
}
